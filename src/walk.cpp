#include<walk.hpp>

int walk::walking(servo leg[4],int x,int y,int z){
    
    int num_legs=sizeof(leg)/sizeof(leg[0]);
    int x_dist=x;
    int y_dist=y;
   
    while(x_dist>0&&y_dist>0){
         
    }
     
    return 0;
}

